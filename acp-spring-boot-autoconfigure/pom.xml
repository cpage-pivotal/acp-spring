<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.thought.acp</groupId>
        <artifactId>acp-spring</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>acp-spring-boot-autoconfigure</artifactId>
    <name>ACP Spring Autoconfigure</name>
    <description>Spring Boot auto-configuration and configuration properties</description>

    <dependencies>
        <dependency>
            <groupId>org.thought.acp</groupId>
            <artifactId>acp-spring-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Runtime adapters are optional: an application puts the one it wants on the classpath. -->
        <dependency>
            <groupId>org.thought.acp</groupId>
            <artifactId>acp-spring-runtime-goose</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.thought.acp</groupId>
            <artifactId>acp-spring-runtime-codex</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.thought.acp</groupId>
            <artifactId>acp-spring-runtime-opencode</artifactId>
            <optional>true</optional>
        </dependency>
        <!-- The generic, registry-driven runtime: any published agent, no adapter. -->
        <dependency>
            <groupId>org.thought.acp</groupId>
            <artifactId>acp-spring-runtime-registry</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Both optional, and both wired only when their types are genuinely on the classpath. -->
        <dependency>
            <groupId>org.thought.acp</groupId>
            <artifactId>acp-spring-ai</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-observation</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- The HTTP endpoint is opt-in twice over: this dependency, then spring.acp.controller.enabled. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
